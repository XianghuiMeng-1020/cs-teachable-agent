[ParsedChatCompletionMessage[ProgrammingProblem](content='{"task_description":"### Task: Spaceship Inventory Management\\n\\nIn this Science Fiction themed task, you need to implement a `Spaceship` class that simulates an inventory management system for a spaceship. The spaceship has a cargo hold where items are stored, and the storage space is limited. Each item has a name, category, and weight.\\n\\nYou must implement the following features:\\n\\n1. **Spaceship** Class:\\n    - **Attributes**:\\n      - `name`: a string representing the name of the spaceship.\\n      - `capacity`: an integer for the maximum weight capacity of the cargo hold.\\n      - `inventory`: a dictionary where keys are item names (strings) and values are dictionaries containing the `category` and `weight` of each item.\\n\\n2. **Methods**:\\n    - `add_item(name, category, weight)`: Adds an item to the inventory. If adding the item exceeds the capacity, raise an `Exception` with the message \\"Insufficient capacity\\". If an item with the same name already exists, update its category and weight.\\n    - `remove_item(name)`: Removes an item from the inventory by its name. If the item does not exist, raise an `Exception` with the message \\"Item not found\\".\\n    - `total_weight()`: Returns the total weight of all items in the cargo hold.\\n    - `list_items()`: Returns a list of tuples, each containing the name, category, and weight of each item in the inventory.\\n\\n### Testing\\n\\nThe test suite will verify that your implementation correctly manages the spaceship\'s inventory as per the above requirements. Your methods will be tested for correct item addition, removal, updating, capacity enforcement, and accurate total weight calculations.\\n\\n### Example Usage\\n\\n```python\\nship = Spaceship(\\"Galactic Falcon\\", 500)\\nship.add_item(\\"Laser Cannon\\", \\"Weapon\\", 60)\\nprint(ship.total_weight()) # Output: 60\\nship.add_item(\\"Laser Cannon\\", \\"Weapon Upgraded\\", 70)\\nprint(ship.list_items()) # Output: [(\\"Laser Cannon\\", \\"Weapon Upgraded\\", 70)]\\n```","test_suite":"import pytest\\nfrom solution import Spaceship\\n\\ndef test_add_and_total_weight():\\n    ship = Spaceship(\\"Galactic Falcon\\", 500)\\n    ship.add_item(\\"Laser Cannon\\", \\"Weapon\\", 60)\\n    assert ship.total_weight() == 60\\n    ship.add_item(\\"Shield Generator\\", \\"Defense\\", 80)\\n    assert ship.total_weight() == 140\\n\\n\\ndef test_item_update():\\n    ship = Spaceship(\\"Galactic Falcon\\", 500)\\n    ship.add_item(\\"Laser Cannon\\", \\"Weapon\\", 60)\\n    ship.add_item(\\"Laser Cannon\\", \\"Weapon Upgraded\\", 70)\\n    assert ship.list_items() == [(\\"Laser Cannon\\", \\"Weapon Upgraded\\", 70)]\\n    assert ship.total_weight() == 70\\n\\n\\ndef test_insufficient_capacity():\\n    ship = Spaceship(\\"Galactic Falcon\\", 100)\\n    ship.add_item(\\"Laser Cannon\\", \\"Weapon\\", 60)\\n    with pytest.raises(Exception) as e:\\n        ship.add_item(\\"Battle Armor\\", \\"Defense\\", 50)\\n    assert str(e.value) == \\"Insufficient capacity\\"\\n\\n\\ndef test_item_removal():\\n    ship = Spaceship(\\"Galactic Falcon\\", 500)\\n    ship.add_item(\\"Laser Cannon\\", \\"Weapon\\", 60)\\n    ship.remove_item(\\"Laser Cannon\\")\\n    assert ship.total_weight() == 0\\n    with pytest.raises(Exception) as e:\\n        ship.remove_item(\\"Laser Cannon\\")\\n    assert str(e.value) == \\"Item not found\\"\\n\\n\\ndef test_list_items():\\n    ship = Spaceship(\\"Galactic Falcon\\", 500)\\n    ship.add_item(\\"Laser Cannon\\", \\"Weapon\\", 60)\\n    ship.add_item(\\"Shield Generator\\", \\"Defense\\", 80)\\n    items = ship.list_items()\\n    expected_items = [\\n        (\\"Laser Cannon\\", \\"Weapon\\", 60),\\n        (\\"Shield Generator\\", \\"Defense\\", 80)\\n    ]\\n    assert items == expected_items\\n","solution_program":"class Spaceship:\\n    def __init__(self, name, capacity):\\n        self.name = name\\n        self.capacity = capacity\\n        self.inventory = {}\\n\\n    def add_item(self, name, category, weight):\\n        current_weight = self.total_weight()\\n        if current_weight + weight > self.capacity:\\n            raise Exception(\\"Insufficient capacity\\")\\n        self.inventory[name] = {\'category\': category, \'weight\': weight}\\n\\n    def remove_item(self, name):\\n        if name not in self.inventory:\\n            raise Exception(\\"Item not found\\")\\n        del self.inventory[name]\\n\\n    def total_weight(self):\\n        return sum(item[\'weight\'] for item in self.inventory.values())\\n\\n    def list_items(self):\\n        return [(name, info[\'category\'], info[\'weight\']) for name, info in self.inventory.items()]"}', refusal=None, role='assistant', function_call=None, tool_calls=[], parsed=ProgrammingProblem(task_description='### Task: Spaceship Inventory Management\n\nIn this Science Fiction themed task, you need to implement a `Spaceship` class that simulates an inventory management system for a spaceship. The spaceship has a cargo hold where items are stored, and the storage space is limited. Each item has a name, category, and weight.\n\nYou must implement the following features:\n\n1. **Spaceship** Class:\n    - **Attributes**:\n      - `name`: a string representing the name of the spaceship.\n      - `capacity`: an integer for the maximum weight capacity of the cargo hold.\n      - `inventory`: a dictionary where keys are item names (strings) and values are dictionaries containing the `category` and `weight` of each item.\n\n2. **Methods**:\n    - `add_item(name, category, weight)`: Adds an item to the inventory. If adding the item exceeds the capacity, raise an `Exception` with the message "Insufficient capacity". If an item with the same name already exists, update its category and weight.\n    - `remove_item(name)`: Removes an item from the inventory by its name. If the item does not exist, raise an `Exception` with the message "Item not found".\n    - `total_weight()`: Returns the total weight of all items in the cargo hold.\n    - `list_items()`: Returns a list of tuples, each containing the name, category, and weight of each item in the inventory.\n\n### Testing\n\nThe test suite will verify that your implementation correctly manages the spaceship\'s inventory as per the above requirements. Your methods will be tested for correct item addition, removal, updating, capacity enforcement, and accurate total weight calculations.\n\n### Example Usage\n\n```python\nship = Spaceship("Galactic Falcon", 500)\nship.add_item("Laser Cannon", "Weapon", 60)\nprint(ship.total_weight()) # Output: 60\nship.add_item("Laser Cannon", "Weapon Upgraded", 70)\nprint(ship.list_items()) # Output: [("Laser Cannon", "Weapon Upgraded", 70)]\n```', test_suite='import pytest\nfrom solution import Spaceship\n\ndef test_add_and_total_weight():\n    ship = Spaceship("Galactic Falcon", 500)\n    ship.add_item("Laser Cannon", "Weapon", 60)\n    assert ship.total_weight() == 60\n    ship.add_item("Shield Generator", "Defense", 80)\n    assert ship.total_weight() == 140\n\n\ndef test_item_update():\n    ship = Spaceship("Galactic Falcon", 500)\n    ship.add_item("Laser Cannon", "Weapon", 60)\n    ship.add_item("Laser Cannon", "Weapon Upgraded", 70)\n    assert ship.list_items() == [("Laser Cannon", "Weapon Upgraded", 70)]\n    assert ship.total_weight() == 70\n\n\ndef test_insufficient_capacity():\n    ship = Spaceship("Galactic Falcon", 100)\n    ship.add_item("Laser Cannon", "Weapon", 60)\n    with pytest.raises(Exception) as e:\n        ship.add_item("Battle Armor", "Defense", 50)\n    assert str(e.value) == "Insufficient capacity"\n\n\ndef test_item_removal():\n    ship = Spaceship("Galactic Falcon", 500)\n    ship.add_item("Laser Cannon", "Weapon", 60)\n    ship.remove_item("Laser Cannon")\n    assert ship.total_weight() == 0\n    with pytest.raises(Exception) as e:\n        ship.remove_item("Laser Cannon")\n    assert str(e.value) == "Item not found"\n\n\ndef test_list_items():\n    ship = Spaceship("Galactic Falcon", 500)\n    ship.add_item("Laser Cannon", "Weapon", 60)\n    ship.add_item("Shield Generator", "Defense", 80)\n    items = ship.list_items()\n    expected_items = [\n        ("Laser Cannon", "Weapon", 60),\n        ("Shield Generator", "Defense", 80)\n    ]\n    assert items == expected_items\n', solution_program='class Spaceship:\n    def __init__(self, name, capacity):\n        self.name = name\n        self.capacity = capacity\n        self.inventory = {}\n\n    def add_item(self, name, category, weight):\n        current_weight = self.total_weight()\n        if current_weight + weight > self.capacity:\n            raise Exception("Insufficient capacity")\n        self.inventory[name] = {\'category\': category, \'weight\': weight}\n\n    def remove_item(self, name):\n        if name not in self.inventory:\n            raise Exception("Item not found")\n        del self.inventory[name]\n\n    def total_weight(self):\n        return sum(item[\'weight\'] for item in self.inventory.values())\n\n    def list_items(self):\n        return [(name, info[\'category\'], info[\'weight\']) for name, info in self.inventory.items()]'), annotations=[]), ParsedChatCompletionMessage[ProgrammingProblem](content='{"task_description":"Imagine a futuristic world where space travelers need to manage their space cargo efficiently. You are tasked with creating a simple inventory management system for a space cargo ship. \\n\\nImplement a class `SpaceCargo` that allows you to manage items in the cargo. Each item should have a name and a quantity, and the inventory should be represented by a dictionary where keys are item names and values are quantities.\\n\\nYour task is to implement the following methods:\\n\\n1. `add_item(name, quantity)`: Adds a specified quantity of an item to the inventory. If the item already exists, it should increase its quantity. If quantity is not a positive integer, raise a ValueError.\\n\\n2. `remove_item(name, quantity)`: Removes a specified quantity of an item from the inventory. If the item does not exist or there is insufficient quantity, raise a KeyError.\\n\\n3. `get_item_quantity(name)`: Returns the current quantity of the specified item. If the item is not found, return 0.\\n\\n4. `list_items()`: Returns a list of all item names in the inventory sorted alphabetically.\\n\\nYour solution should handle exceptions properly, ensuring the program does not crash during invalid operations.","test_suite":"import pytest\\nfrom solution import SpaceCargo\\n\\ndef setup_module(module):\\n    module.ship_cargo = SpaceCargo()\\n    ship_cargo.add_item(\\"Oxygen\\", 50)\\n    ship_cargo.add_item(\\"Food\\", 20)\\n\\n\\ndef teardown_module(module):\\n    del module.ship_cargo\\n\\n\\ndef test_add_item_new():\\n    cargo = SpaceCargo()\\n    cargo.add_item(\\"Fuel\\", 100)\\n    assert cargo.get_item_quantity(\\"Fuel\\") == 100\\n\\n\\ndef test_add_item_existing():\\n    ship_cargo.add_item(\\"Oxygen\\", 30)\\n    assert ship_cargo.get_item_quantity(\\"Oxygen\\") == 80\\n\\n\\ndef test_add_item_invalid_quantity():\\n    with pytest.raises(ValueError):\\n        ship_cargo.add_item(\\"Helium\\", -10)\\n\\n\\ndef test_remove_item_sufficient():\\n    ship_cargo.remove_item(\\"Food\\", 10)\\n    assert ship_cargo.get_item_quantity(\\"Food\\") == 10\\n\\n\\ndef test_remove_item_insufficient():\\n    with pytest.raises(KeyError):\\n        ship_cargo.remove_item(\\"Food\\", 30)\\n\\n\\ndef test_remove_item_nonexistent():\\n    with pytest.raises(KeyError):\\n        ship_cargo.remove_item(\\"Water\\", 10)\\n\\n\\ndef test_get_item_quantity_nonexistent():\\n    assert ship_cargo.get_item_quantity(\\"Water\\") == 0\\n\\n\\ndef test_list_items():\\n    expected_items = sorted([\\"Food\\", \\"Oxygen\\"])\\n    assert ship_cargo.list_items() == expected_items","solution_program":"class SpaceCargo:\\n    def __init__(self):\\n        self.inventory = {}\\n\\n    def add_item(self, name, quantity):\\n        if not isinstance(quantity, int) or quantity <= 0:\\n            raise ValueError(\\"Quantity must be a positive integer.\\")\\n        if name in self.inventory:\\n            self.inventory[name] += quantity\\n        else:\\n            self.inventory[name] = quantity\\n\\n    def remove_item(self, name, quantity):\\n        if name not in self.inventory or self.inventory[name] < quantity:\\n            raise KeyError(\\"Item not found or insufficient quantity.\\")\\n        self.inventory[name] -= quantity\\n        if self.inventory[name] == 0:\\n            del self.inventory[name]\\n\\n    def get_item_quantity(self, name):\\n        return self.inventory.get(name, 0)\\n\\n    def list_items(self):\\n        return sorted(self.inventory.keys())"}', refusal=None, role='assistant', function_call=None, tool_calls=[], parsed=ProgrammingProblem(task_description='Imagine a futuristic world where space travelers need to manage their space cargo efficiently. You are tasked with creating a simple inventory management system for a space cargo ship. \n\nImplement a class `SpaceCargo` that allows you to manage items in the cargo. Each item should have a name and a quantity, and the inventory should be represented by a dictionary where keys are item names and values are quantities.\n\nYour task is to implement the following methods:\n\n1. `add_item(name, quantity)`: Adds a specified quantity of an item to the inventory. If the item already exists, it should increase its quantity. If quantity is not a positive integer, raise a ValueError.\n\n2. `remove_item(name, quantity)`: Removes a specified quantity of an item from the inventory. If the item does not exist or there is insufficient quantity, raise a KeyError.\n\n3. `get_item_quantity(name)`: Returns the current quantity of the specified item. If the item is not found, return 0.\n\n4. `list_items()`: Returns a list of all item names in the inventory sorted alphabetically.\n\nYour solution should handle exceptions properly, ensuring the program does not crash during invalid operations.', test_suite='import pytest\nfrom solution import SpaceCargo\n\ndef setup_module(module):\n    module.ship_cargo = SpaceCargo()\n    ship_cargo.add_item("Oxygen", 50)\n    ship_cargo.add_item("Food", 20)\n\n\ndef teardown_module(module):\n    del module.ship_cargo\n\n\ndef test_add_item_new():\n    cargo = SpaceCargo()\n    cargo.add_item("Fuel", 100)\n    assert cargo.get_item_quantity("Fuel") == 100\n\n\ndef test_add_item_existing():\n    ship_cargo.add_item("Oxygen", 30)\n    assert ship_cargo.get_item_quantity("Oxygen") == 80\n\n\ndef test_add_item_invalid_quantity():\n    with pytest.raises(ValueError):\n        ship_cargo.add_item("Helium", -10)\n\n\ndef test_remove_item_sufficient():\n    ship_cargo.remove_item("Food", 10)\n    assert ship_cargo.get_item_quantity("Food") == 10\n\n\ndef test_remove_item_insufficient():\n    with pytest.raises(KeyError):\n        ship_cargo.remove_item("Food", 30)\n\n\ndef test_remove_item_nonexistent():\n    with pytest.raises(KeyError):\n        ship_cargo.remove_item("Water", 10)\n\n\ndef test_get_item_quantity_nonexistent():\n    assert ship_cargo.get_item_quantity("Water") == 0\n\n\ndef test_list_items():\n    expected_items = sorted(["Food", "Oxygen"])\n    assert ship_cargo.list_items() == expected_items', solution_program='class SpaceCargo:\n    def __init__(self):\n        self.inventory = {}\n\n    def add_item(self, name, quantity):\n        if not isinstance(quantity, int) or quantity <= 0:\n            raise ValueError("Quantity must be a positive integer.")\n        if name in self.inventory:\n            self.inventory[name] += quantity\n        else:\n            self.inventory[name] = quantity\n\n    def remove_item(self, name, quantity):\n        if name not in self.inventory or self.inventory[name] < quantity:\n            raise KeyError("Item not found or insufficient quantity.")\n        self.inventory[name] -= quantity\n        if self.inventory[name] == 0:\n            del self.inventory[name]\n\n    def get_item_quantity(self, name):\n        return self.inventory.get(name, 0)\n\n    def list_items(self):\n        return sorted(self.inventory.keys())'), annotations=[]), ParsedChatCompletionMessage[ProgrammingProblem](content='{"task_description":"### Task Description\\n\\nIn a science fiction universe, a fleet of spaceships is involved in an intergalactic race event. You are tasked with creating a program to manage the fleet of spaceships being tracked in this event. Each spaceship has specific attributes such as its name, speed, and current fuel level. You will implement a class `SpaceshipFleet` to handle the fleet operations.\\n\\nCreate a class `Spaceship` with the following attributes:\\n- `name`: the name of the spaceship (a string).\\n- `speed`: speed of the spaceship in light-years per hour (an integer).\\n- `fuel`: current fuel level in percentage (an integer).\\n\\nImplement the `SpaceshipFleet` class with the following methods:\\n\\n1. **add_spaceship(name, speed, fuel):**\\n   - Adds a new spaceship to the fleet.\\n   - Raises a `ValueError` if a spaceship with the same name already exists.\\n\\n2. **remove_spaceship(name):**\\n   - Removes a spaceship with the given name from the fleet.\\n   - Raises a `KeyError` if no spaceship with that name is found.\\n\\n3. **get_fastest_spaceship():**\\n   - Returns the name of the spaceship with the highest speed.\\n   - Raises a `RuntimeError` if no spaceships are in the fleet.\\n\\n4. **get_fuel_status():**\\n   - Returns a list of dictionaries, each containing the name of the spaceship and its current fuel level, sorted by name.\\n\\n### Testing\\n\\nThe class will be tested using the `pytest` framework, ensuring the functionality of the methods and the correct handling of exceptions.","test_suite":"import pytest\\nfrom solution import SpaceshipFleet, Spaceship\\n\\n@pytest.fixture\\ndef fleet():\\n    return SpaceshipFleet()\\n\\n# Test adding spaceships and handling duplicates\\n\\ndef test_add_spaceship(fleet):\\n    fleet.add_spaceship(\\"Andromeda\\", 500, 80)\\n    fleet.add_spaceship(\\"Falcon\\", 600, 90)\\n    assert len(fleet.ships) == 2\\n    with pytest.raises(ValueError):\\n        fleet.add_spaceship(\\"Andromeda\\", 700, 70)\\n\\n# Test removing a spaceship and handling non-existent spaceship\\n\\ndef test_remove_spaceship(fleet):\\n    fleet.add_spaceship(\\"Andromeda\\", 500, 80)\\n    fleet.remove_spaceship(\\"Andromeda\\")\\n    assert len(fleet.ships) == 0\\n    with pytest.raises(KeyError):\\n        fleet.remove_spaceship(\\"Andromeda\\")\\n\\n# Test getting the fastest spaceship\\n\\ndef test_get_fastest_spaceship(fleet):\\n    fleet.add_spaceship(\\"Andromeda\\", 500, 80)\\n    fleet.add_spaceship(\\"Zephyr\\", 550, 60)\\n    fleet.add_spaceship(\\"Falcon\\", 600, 90)\\n    assert fleet.get_fastest_spaceship() == \\"Falcon\\"\\n    fleet.remove_spaceship(\\"Falcon\\")\\n    assert fleet.get_fastest_spaceship() == \\"Zephyr\\"\\n    fleet.remove_spaceship(\\"Andromeda\\")\\n    fleet.remove_spaceship(\\"Zephyr\\")\\n    with pytest.raises(RuntimeError):\\n        fleet.get_fastest_spaceship()\\n\\n# Test getting fuel status\\n\\ndef test_get_fuel_status(fleet):\\n    fleet.add_spaceship(\\"Andromeda\\", 500, 80)\\n    fleet.add_spaceship(\\"Zephyr\\", 550, 60)\\n    fuel_status = fleet.get_fuel_status()\\n    expected = [\\n        {\'name\': \'Andromeda\', \'fuel\': 80},\\n        {\'name\': \'Zephyr\', \'fuel\': 60}\\n    ]\\n    assert fuel_status == expected\\n","solution_program":"class Spaceship:\\n    def __init__(self, name, speed, fuel):\\n        self.name = name\\n        self.speed = speed\\n        self.fuel = fuel\\n\\nclass SpaceshipFleet:\\n    def __init__(self):\\n        self.ships = {}\\n\\n    def add_spaceship(self, name, speed, fuel):\\n        if name in self.ships:\\n            raise ValueError(\\"Spaceship with this name already exists.\\")\\n        self.ships[name] = Spaceship(name, speed, fuel)\\n\\n    def remove_spaceship(self, name):\\n        if name not in self.ships:\\n            raise KeyError(\\"Spaceship with this name does not exist.\\")\\n        del self.ships[name]\\n\\n    def get_fastest_spaceship(self):\\n        if not self.ships:\\n            raise RuntimeError(\\"No spaceships in fleet.\\")\\n        max_speed_ship = max(self.ships.values(), key=lambda ship: ship.speed)\\n        return max_speed_ship.name\\n\\n    def get_fuel_status(self):\\n        return sorted(\\n            [{\'name\': ship.name, \'fuel\': ship.fuel} for ship in self.ships.values()],\\n            key=lambda x: x[\'name\']\\n        )\\n"}', refusal=None, role='assistant', function_call=None, tool_calls=[], parsed=ProgrammingProblem(task_description='### Task Description\n\nIn a science fiction universe, a fleet of spaceships is involved in an intergalactic race event. You are tasked with creating a program to manage the fleet of spaceships being tracked in this event. Each spaceship has specific attributes such as its name, speed, and current fuel level. You will implement a class `SpaceshipFleet` to handle the fleet operations.\n\nCreate a class `Spaceship` with the following attributes:\n- `name`: the name of the spaceship (a string).\n- `speed`: speed of the spaceship in light-years per hour (an integer).\n- `fuel`: current fuel level in percentage (an integer).\n\nImplement the `SpaceshipFleet` class with the following methods:\n\n1. **add_spaceship(name, speed, fuel):**\n   - Adds a new spaceship to the fleet.\n   - Raises a `ValueError` if a spaceship with the same name already exists.\n\n2. **remove_spaceship(name):**\n   - Removes a spaceship with the given name from the fleet.\n   - Raises a `KeyError` if no spaceship with that name is found.\n\n3. **get_fastest_spaceship():**\n   - Returns the name of the spaceship with the highest speed.\n   - Raises a `RuntimeError` if no spaceships are in the fleet.\n\n4. **get_fuel_status():**\n   - Returns a list of dictionaries, each containing the name of the spaceship and its current fuel level, sorted by name.\n\n### Testing\n\nThe class will be tested using the `pytest` framework, ensuring the functionality of the methods and the correct handling of exceptions.', test_suite='import pytest\nfrom solution import SpaceshipFleet, Spaceship\n\n@pytest.fixture\ndef fleet():\n    return SpaceshipFleet()\n\n# Test adding spaceships and handling duplicates\n\ndef test_add_spaceship(fleet):\n    fleet.add_spaceship("Andromeda", 500, 80)\n    fleet.add_spaceship("Falcon", 600, 90)\n    assert len(fleet.ships) == 2\n    with pytest.raises(ValueError):\n        fleet.add_spaceship("Andromeda", 700, 70)\n\n# Test removing a spaceship and handling non-existent spaceship\n\ndef test_remove_spaceship(fleet):\n    fleet.add_spaceship("Andromeda", 500, 80)\n    fleet.remove_spaceship("Andromeda")\n    assert len(fleet.ships) == 0\n    with pytest.raises(KeyError):\n        fleet.remove_spaceship("Andromeda")\n\n# Test getting the fastest spaceship\n\ndef test_get_fastest_spaceship(fleet):\n    fleet.add_spaceship("Andromeda", 500, 80)\n    fleet.add_spaceship("Zephyr", 550, 60)\n    fleet.add_spaceship("Falcon", 600, 90)\n    assert fleet.get_fastest_spaceship() == "Falcon"\n    fleet.remove_spaceship("Falcon")\n    assert fleet.get_fastest_spaceship() == "Zephyr"\n    fleet.remove_spaceship("Andromeda")\n    fleet.remove_spaceship("Zephyr")\n    with pytest.raises(RuntimeError):\n        fleet.get_fastest_spaceship()\n\n# Test getting fuel status\n\ndef test_get_fuel_status(fleet):\n    fleet.add_spaceship("Andromeda", 500, 80)\n    fleet.add_spaceship("Zephyr", 550, 60)\n    fuel_status = fleet.get_fuel_status()\n    expected = [\n        {\'name\': \'Andromeda\', \'fuel\': 80},\n        {\'name\': \'Zephyr\', \'fuel\': 60}\n    ]\n    assert fuel_status == expected\n', solution_program='class Spaceship:\n    def __init__(self, name, speed, fuel):\n        self.name = name\n        self.speed = speed\n        self.fuel = fuel\n\nclass SpaceshipFleet:\n    def __init__(self):\n        self.ships = {}\n\n    def add_spaceship(self, name, speed, fuel):\n        if name in self.ships:\n            raise ValueError("Spaceship with this name already exists.")\n        self.ships[name] = Spaceship(name, speed, fuel)\n\n    def remove_spaceship(self, name):\n        if name not in self.ships:\n            raise KeyError("Spaceship with this name does not exist.")\n        del self.ships[name]\n\n    def get_fastest_spaceship(self):\n        if not self.ships:\n            raise RuntimeError("No spaceships in fleet.")\n        max_speed_ship = max(self.ships.values(), key=lambda ship: ship.speed)\n        return max_speed_ship.name\n\n    def get_fuel_status(self):\n        return sorted(\n            [{\'name\': ship.name, \'fuel\': ship.fuel} for ship in self.ships.values()],\n            key=lambda x: x[\'name\']\n        )\n'), annotations=[]), ParsedChatCompletionMessage[ProgrammingProblem](content='{"task_description":"You are tasked with creating a system for managing a fleet of spaceships. Each spaceship is commanded by a captain and can perform various missions such as reconnaissance, transport, or combat. Your task is to implement a class `Spaceship` that manages these attributes and actions.\\n\\nThe class `Spaceship` should have the following functionalities:\\n\\n- **Attributes**:\\n  - `name`: A string representing the name of the spaceship, which is required during instantiation.\\n  - `captain`: A string representing the name of the spaceship\'s captain, which is optional during instantiation. If not provided, it defaults to \'Unknown\'.\\n  - `missions`: A list to store completed missions. It is initially empty.\\n\\n- **Methods**:\\n  - `add_mission(mission_type)`: This method takes a mission type as argument (a string) and adds it to the missions list. Valid mission types are \\"Reconnaissance\\", \\"Transport\\", and \\"Combat\\". If the mission type is invalid, raise a `ValueError` with the message \\"Invalid mission type\\".\\n  - `get_missions_summary()`: This method returns a dictionary summarizing completed missions. The keys should be \\"Reconnaissance\\", \\"Transport\\", and \\"Combat\\", and the values should be the counts of each corresponding mission completed by the spaceship.\\n\\nImplement this class and ensure it performs as described.\\n\\nThe class should support the following operations:\\n\\n- Instantiating a spaceship with or without a captain.\\n- Adding missions and handling invalid inputs using exception handling.\\n- Retrieving a summary of all missions completed by the spaceship.\\n\\nYour class will be tested using automated tests that will check the functionality as described.","test_suite":"import pytest\\nfrom solution import Spaceship\\n\\n\\ndef test_create_spaceship_with_captain():\\n    ship = Spaceship(name=\\"Endeavor\\", captain=\\"James T.\\")\\n    assert ship.name == \\"Endeavor\\"\\n    assert ship.captain == \\"James T.\\"\\n    assert ship.missions == []\\n\\n\\ndef test_create_spaceship_without_captain():\\n    ship = Spaceship(name=\\"Discovery\\")\\n    assert ship.name == \\"Discovery\\"\\n    assert ship.captain == \\"Unknown\\"\\n    assert ship.missions == []\\n\\n\\ndef test_add_valid_missions():\\n    ship = Spaceship(name=\\"Voyager\\")\\n    ship.add_mission(\\"Reconnaissance\\")\\n    ship.add_mission(\\"Transport\\")\\n    ship.add_mission(\\"Combat\\")\\n    assert ship.missions == [\\"Reconnaissance\\", \\"Transport\\", \\"Combat\\"]\\n    summary = ship.get_missions_summary()\\n    assert summary == {\\"Reconnaissance\\": 1, \\"Transport\\": 1, \\"Combat\\": 1}\\n\\n\\ndef test_add_invalid_mission_raises_exception():\\n    ship = Spaceship(name=\\"Enterprise\\")\\n    with pytest.raises(ValueError) as excinfo:\\n        ship.add_mission(\\"Exploration\\")\\n    assert \\"Invalid mission type\\" in str(excinfo.value)\\n\\n\\ndef test_mission_summary():\\n    ship = Spaceship(name=\\"Atlantis\\")\\n    ship.add_mission(\\"Reconnaissance\\")\\n    ship.add_mission(\\"Reconnaissance\\")\\n    ship.add_mission(\\"Combat\\")\\n    summary = ship.get_missions_summary()\\n    assert summary == {\\"Reconnaissance\\": 2, \\"Transport\\": 0, \\"Combat\\": 1}","solution_program":"class Spaceship:\\n    def __init__(self, name, captain=\'Unknown\'):\\n        self.name = name\\n        self.captain = captain\\n        self.missions = []\\n\\n    def add_mission(self, mission_type):\\n        valid_missions = [\\"Reconnaissance\\", \\"Transport\\", \\"Combat\\"]\\n        if mission_type not in valid_missions:\\n            raise ValueError(\\"Invalid mission type\\")\\n        self.missions.append(mission_type)\\n\\n    def get_missions_summary(self):\\n        summary = {\\"Reconnaissance\\": 0, \\"Transport\\": 0, \\"Combat\\": 0}\\n        for mission in self.missions:\\n            if mission in summary:\\n                summary[mission] += 1\\n        return summary"}', refusal=None, role='assistant', function_call=None, tool_calls=[], parsed=ProgrammingProblem(task_description='You are tasked with creating a system for managing a fleet of spaceships. Each spaceship is commanded by a captain and can perform various missions such as reconnaissance, transport, or combat. Your task is to implement a class `Spaceship` that manages these attributes and actions.\n\nThe class `Spaceship` should have the following functionalities:\n\n- **Attributes**:\n  - `name`: A string representing the name of the spaceship, which is required during instantiation.\n  - `captain`: A string representing the name of the spaceship\'s captain, which is optional during instantiation. If not provided, it defaults to \'Unknown\'.\n  - `missions`: A list to store completed missions. It is initially empty.\n\n- **Methods**:\n  - `add_mission(mission_type)`: This method takes a mission type as argument (a string) and adds it to the missions list. Valid mission types are "Reconnaissance", "Transport", and "Combat". If the mission type is invalid, raise a `ValueError` with the message "Invalid mission type".\n  - `get_missions_summary()`: This method returns a dictionary summarizing completed missions. The keys should be "Reconnaissance", "Transport", and "Combat", and the values should be the counts of each corresponding mission completed by the spaceship.\n\nImplement this class and ensure it performs as described.\n\nThe class should support the following operations:\n\n- Instantiating a spaceship with or without a captain.\n- Adding missions and handling invalid inputs using exception handling.\n- Retrieving a summary of all missions completed by the spaceship.\n\nYour class will be tested using automated tests that will check the functionality as described.', test_suite='import pytest\nfrom solution import Spaceship\n\n\ndef test_create_spaceship_with_captain():\n    ship = Spaceship(name="Endeavor", captain="James T.")\n    assert ship.name == "Endeavor"\n    assert ship.captain == "James T."\n    assert ship.missions == []\n\n\ndef test_create_spaceship_without_captain():\n    ship = Spaceship(name="Discovery")\n    assert ship.name == "Discovery"\n    assert ship.captain == "Unknown"\n    assert ship.missions == []\n\n\ndef test_add_valid_missions():\n    ship = Spaceship(name="Voyager")\n    ship.add_mission("Reconnaissance")\n    ship.add_mission("Transport")\n    ship.add_mission("Combat")\n    assert ship.missions == ["Reconnaissance", "Transport", "Combat"]\n    summary = ship.get_missions_summary()\n    assert summary == {"Reconnaissance": 1, "Transport": 1, "Combat": 1}\n\n\ndef test_add_invalid_mission_raises_exception():\n    ship = Spaceship(name="Enterprise")\n    with pytest.raises(ValueError) as excinfo:\n        ship.add_mission("Exploration")\n    assert "Invalid mission type" in str(excinfo.value)\n\n\ndef test_mission_summary():\n    ship = Spaceship(name="Atlantis")\n    ship.add_mission("Reconnaissance")\n    ship.add_mission("Reconnaissance")\n    ship.add_mission("Combat")\n    summary = ship.get_missions_summary()\n    assert summary == {"Reconnaissance": 2, "Transport": 0, "Combat": 1}', solution_program='class Spaceship:\n    def __init__(self, name, captain=\'Unknown\'):\n        self.name = name\n        self.captain = captain\n        self.missions = []\n\n    def add_mission(self, mission_type):\n        valid_missions = ["Reconnaissance", "Transport", "Combat"]\n        if mission_type not in valid_missions:\n            raise ValueError("Invalid mission type")\n        self.missions.append(mission_type)\n\n    def get_missions_summary(self):\n        summary = {"Reconnaissance": 0, "Transport": 0, "Combat": 0}\n        for mission in self.missions:\n            if mission in summary:\n                summary[mission] += 1\n        return summary'), annotations=[]), ParsedChatCompletionMessage[ProgrammingProblem](content='{"task_description":"You are tasked with developing a program that manages a fleet of science fiction spaceships. Each spaceship is represented as an object of the class `Spaceship`.\\n\\nEach `Spaceship` instance has the following properties:\\n- `name`: The name of the spaceship (a string).\\n- `energy_level`: A floating-point number representing the energy level of the spaceship. This should be a non-negative value, with a maximum of 100.\\n- `crew_members`: A dictionary where the keys are crew member names (as strings) and the values are their roles (also strings).\\n\\nImplement a class `Spaceship` with the following methods:\\n- `__init__(self, name, energy_level, crew_members)`: Initializes the spaceship with a name, an initial energy level, and a dictionary of crew members.\\n- `refuel(self, amount)`: Increases the spaceship\'s energy level by `amount`. If the resulting energy level exceeds 100, set it to 100.\\n- `assign_crew_member(self, name, role)`: Assigns a crew member to the spaceship with the specified name and role. Add them to the `crew_members` dictionary.\\n- `remove_crew_member(self, name)`: Removes a crew member by their name from the `crew_members` dictionary. Raise a `ValueError` if the crew member does not exist.\\n- `status_report(self)`: Returns a dictionary containing the spaceship\'s name, current energy level, and list of crew members with their roles.\\n\\nYour task is to correctly implement the `Spaceship` class and the methods specified. The program will be tested using a series of unit tests to verify its correctness and reliability.","test_suite":"import pytest\\nfrom solution import Spaceship\\n\\n@pytest.fixture\\ndef spaceship():\\n    return Spaceship(\'Odyssey\', 50.0, {\'Alice\': \'Pilot\', \'Bob\': \'Engineer\'})\\n\\ndef test_initial_status(spaceship):\\n    report = spaceship.status_report()\\n    assert report[\'name\'] == \'Odyssey\'\\n    assert report[\'energy_level\'] == 50.0\\n    assert report[\'crew_members\'] == {\'Alice\': \'Pilot\', \'Bob\': \'Engineer\'}\\n\\ndef test_refuel(spaceship):\\n    spaceship.refuel(30.0)\\n    assert spaceship.energy_level == 80.0\\n    spaceship.refuel(30.0)\\n    assert spaceship.energy_level == 100.0\\n\\ndef test_assign_crew_member(spaceship):\\n    spaceship.assign_crew_member(\'Charlie\', \'Navigator\')\\n    assert \'Charlie\' in spaceship.crew_members\\n    assert spaceship.crew_members[\'Charlie\'] == \'Navigator\'\\n\\ndef test_remove_crew_member(spaceship):\\n    spaceship.remove_crew_member(\'Alice\')\\n    assert \'Alice\' not in spaceship.crew_members\\n\\n    with pytest.raises(ValueError):\\n        spaceship.remove_crew_member(\'Nonexistent\')\\n\\n    # Ensure Bob still exists\\n    assert \'Bob\' in spaceship.crew_members\\n\\n    spaceship.remove_crew_member(\'Bob\')\\n    assert \'Bob\' not in spaceship.crew_members\\n\\n    # Now all crew should be removed\\n    assert spaceship.crew_members == {}\\n\\ndef test_maximum_energy_level(spaceship):\\n    spaceship.refuel(1000)\\n    assert spaceship.energy_level == 100.0\\n\\n    spaceship.refuel(-150)\\n    assert spaceship.energy_level == 0","solution_program":"class Spaceship:\\n    def __init__(self, name, energy_level, crew_members):\\n        self.name = name\\n        self.energy_level = max(0, min(energy_level, 100))\\n        self.crew_members = crew_members\\n\\n    def refuel(self, amount):\\n        self.energy_level += amount\\n        if self.energy_level > 100:\\n            self.energy_level = 100\\n        elif self.energy_level < 0:\\n            self.energy_level = 0\\n\\n    def assign_crew_member(self, name, role):\\n        self.crew_members[name] = role\\n\\n    def remove_crew_member(self, name):\\n        if name in self.crew_members:\\n            del self.crew_members[name]\\n        else:\\n            raise ValueError(\'Crew member does not exist\')\\n\\n    def status_report(self):\\n        return {\\n            \'name\': self.name,\\n            \'energy_level\': self.energy_level,\\n            \'crew_members\': self.crew_members.copy()\\n        }"}', refusal=None, role='assistant', function_call=None, tool_calls=[], parsed=ProgrammingProblem(task_description="You are tasked with developing a program that manages a fleet of science fiction spaceships. Each spaceship is represented as an object of the class `Spaceship`.\n\nEach `Spaceship` instance has the following properties:\n- `name`: The name of the spaceship (a string).\n- `energy_level`: A floating-point number representing the energy level of the spaceship. This should be a non-negative value, with a maximum of 100.\n- `crew_members`: A dictionary where the keys are crew member names (as strings) and the values are their roles (also strings).\n\nImplement a class `Spaceship` with the following methods:\n- `__init__(self, name, energy_level, crew_members)`: Initializes the spaceship with a name, an initial energy level, and a dictionary of crew members.\n- `refuel(self, amount)`: Increases the spaceship's energy level by `amount`. If the resulting energy level exceeds 100, set it to 100.\n- `assign_crew_member(self, name, role)`: Assigns a crew member to the spaceship with the specified name and role. Add them to the `crew_members` dictionary.\n- `remove_crew_member(self, name)`: Removes a crew member by their name from the `crew_members` dictionary. Raise a `ValueError` if the crew member does not exist.\n- `status_report(self)`: Returns a dictionary containing the spaceship's name, current energy level, and list of crew members with their roles.\n\nYour task is to correctly implement the `Spaceship` class and the methods specified. The program will be tested using a series of unit tests to verify its correctness and reliability.", test_suite="import pytest\nfrom solution import Spaceship\n\n@pytest.fixture\ndef spaceship():\n    return Spaceship('Odyssey', 50.0, {'Alice': 'Pilot', 'Bob': 'Engineer'})\n\ndef test_initial_status(spaceship):\n    report = spaceship.status_report()\n    assert report['name'] == 'Odyssey'\n    assert report['energy_level'] == 50.0\n    assert report['crew_members'] == {'Alice': 'Pilot', 'Bob': 'Engineer'}\n\ndef test_refuel(spaceship):\n    spaceship.refuel(30.0)\n    assert spaceship.energy_level == 80.0\n    spaceship.refuel(30.0)\n    assert spaceship.energy_level == 100.0\n\ndef test_assign_crew_member(spaceship):\n    spaceship.assign_crew_member('Charlie', 'Navigator')\n    assert 'Charlie' in spaceship.crew_members\n    assert spaceship.crew_members['Charlie'] == 'Navigator'\n\ndef test_remove_crew_member(spaceship):\n    spaceship.remove_crew_member('Alice')\n    assert 'Alice' not in spaceship.crew_members\n\n    with pytest.raises(ValueError):\n        spaceship.remove_crew_member('Nonexistent')\n\n    # Ensure Bob still exists\n    assert 'Bob' in spaceship.crew_members\n\n    spaceship.remove_crew_member('Bob')\n    assert 'Bob' not in spaceship.crew_members\n\n    # Now all crew should be removed\n    assert spaceship.crew_members == {}\n\ndef test_maximum_energy_level(spaceship):\n    spaceship.refuel(1000)\n    assert spaceship.energy_level == 100.0\n\n    spaceship.refuel(-150)\n    assert spaceship.energy_level == 0", solution_program="class Spaceship:\n    def __init__(self, name, energy_level, crew_members):\n        self.name = name\n        self.energy_level = max(0, min(energy_level, 100))\n        self.crew_members = crew_members\n\n    def refuel(self, amount):\n        self.energy_level += amount\n        if self.energy_level > 100:\n            self.energy_level = 100\n        elif self.energy_level < 0:\n            self.energy_level = 0\n\n    def assign_crew_member(self, name, role):\n        self.crew_members[name] = role\n\n    def remove_crew_member(self, name):\n        if name in self.crew_members:\n            del self.crew_members[name]\n        else:\n            raise ValueError('Crew member does not exist')\n\n    def status_report(self):\n        return {\n            'name': self.name,\n            'energy_level': self.energy_level,\n            'crew_members': self.crew_members.copy()\n        }"), annotations=[]), ParsedChatCompletionMessage[ProgrammingProblem](content='{"task_description":"### Task Description\\n\\nIn the distant future, aboard a space station orbiting around a newly discovered planet \\"Zeta-X9\\", scientists have developed a sophisticated system to monitor different types of celestial phenomena. As a software engineer on the station, your task is to develop a Python program to manage and process information about these phenomena.\\n\\nYou need to create a class `PhenomenaMonitor` that maintains a list of phenomena, where each phenomenon is described by a dictionary with the following keys:\\n- `type`: a string representing the type of the phenomenon (e.g., \\"Aurora\\", \\"Supernova\\")\\n- `severity`: a string describing the severity level (e.g., \\"low\\", \\"medium\\", \\"high\\")\\n- `observation_notes`: a list of strings containing notes taken during observations\\n\\nThe class should support the following operations:\\n\\n1. `add_phenomenon(phenomenon)`: Add the given phenomenon (passed as a dictionary) to the list.\\n\\n2. `get_phenomena_by_type(phenomenon_type)`: Retrieve all phenomena of a given type and return them as a list.\\n\\n3. `get_severity_count()`: Return a dictionary with the count of phenomena for each severity level.\\n\\n4. `add_observation(phenomenon_index, note)`: Add a new observation note to a phenomenon identified by its index in the list. If the index is invalid, handle the exception and continue gracefully.\\n\\nImplement the `PhenomenaMonitor` class and ensure that it works smoothly with a variety of input scenarios.","test_suite":"import pytest\\n\\nfrom solution import PhenomenaMonitor\\n\\n@pytest.fixture\\ndef monitor():\\n    return PhenomenaMonitor()\\n\\n\\ndef test_add_phenomenon_and_retrieve_by_type(monitor):\\n    monitor.add_phenomenon({\\"type\\": \\"Aurora\\", \\"severity\\": \\"low\\", \\"observation_notes\\": []})\\n    monitor.add_phenomenon({\\"type\\": \\"Supernova\\", \\"severity\\": \\"high\\", \\"observation_notes\\": []})\\n    auroras = monitor.get_phenomena_by_type(\\"Aurora\\")\\n    assert len(auroras) == 1\\n    assert auroras[0][\\"type\\"] == \\"Aurora\\"\\n\\ndef test_get_severity_count(monitor):\\n    monitor.add_phenomenon({\\"type\\": \\"Aurora\\", \\"severity\\": \\"low\\", \\"observation_notes\\": []})\\n    monitor.add_phenomenon({\\"type\\": \\"Supernova\\", \\"severity\\": \\"high\\", \\"observation_notes\\": []})\\n    monitor.add_phenomenon({\\"type\\": \\"Meteor Shower\\", \\"severity\\": \\"medium\\", \\"observation_notes\\": []})\\n    severity_count = monitor.get_severity_count()\\n    assert severity_count == {\\"low\\": 1, \\"medium\\": 1, \\"high\\": 1}\\n\\ndef test_add_observation(monitor):\\n    monitor.add_phenomenon({\\"type\\": \\"Aurora\\", \\"severity\\": \\"low\\", \\"observation_notes\\": []})\\n    monitor.add_observation(0, \\"Observed during night shift.\\")\\n    auroras = monitor.get_phenomena_by_type(\\"Aurora\\")\\n    assert auroras[0][\\"observation_notes\\"] == [\\"Observed during night shift.\\"]\\n\\ndef test_add_observation_invalid_index_gracefully(monitor):\\n    monitor.add_phenomenon({\\"type\\": \\"Aurora\\", \\"severity\\": \\"low\\", \\"observation_notes\\": []})\\n    monitor.add_observation(10, \\"Impossible observation.\\")  # Invalid index\\n    auroras = monitor.get_phenomena_by_type(\\"Aurora\\")\\n    assert auroras[0][\\"observation_notes\\"] == []\\n\\ndef test_empty_severity_count(monitor):\\n    severity_count = monitor.get_severity_count()\\n    assert severity_count == {}","solution_program":"class PhenomenaMonitor:\\n    def __init__(self):\\n        self.phenomena = []\\n\\n    def add_phenomenon(self, phenomenon):\\n        self.phenomena.append(phenomenon)\\n\\n    def get_phenomena_by_type(self, phenomenon_type):\\n        return [p for p in self.phenomena if p[\'type\'] == phenomenon_type]\\n\\n    def get_severity_count(self):\\n        severity_count = {}\\n        for phenomenon in self.phenomena:\\n            severity = phenomenon[\'severity\']\\n            if severity in severity_count:\\n                severity_count[severity] += 1\\n            else:\\n                severity_count[severity] = 1\\n        return severity_count\\n\\n    def add_observation(self, phenomenon_index, note):\\n        try:\\n            self.phenomena[phenomenon_index][\'observation_notes\'].append(note)\\n        except IndexError:\\n            pass"}', refusal=None, role='assistant', function_call=None, tool_calls=[], parsed=ProgrammingProblem(task_description='### Task Description\n\nIn the distant future, aboard a space station orbiting around a newly discovered planet "Zeta-X9", scientists have developed a sophisticated system to monitor different types of celestial phenomena. As a software engineer on the station, your task is to develop a Python program to manage and process information about these phenomena.\n\nYou need to create a class `PhenomenaMonitor` that maintains a list of phenomena, where each phenomenon is described by a dictionary with the following keys:\n- `type`: a string representing the type of the phenomenon (e.g., "Aurora", "Supernova")\n- `severity`: a string describing the severity level (e.g., "low", "medium", "high")\n- `observation_notes`: a list of strings containing notes taken during observations\n\nThe class should support the following operations:\n\n1. `add_phenomenon(phenomenon)`: Add the given phenomenon (passed as a dictionary) to the list.\n\n2. `get_phenomena_by_type(phenomenon_type)`: Retrieve all phenomena of a given type and return them as a list.\n\n3. `get_severity_count()`: Return a dictionary with the count of phenomena for each severity level.\n\n4. `add_observation(phenomenon_index, note)`: Add a new observation note to a phenomenon identified by its index in the list. If the index is invalid, handle the exception and continue gracefully.\n\nImplement the `PhenomenaMonitor` class and ensure that it works smoothly with a variety of input scenarios.', test_suite='import pytest\n\nfrom solution import PhenomenaMonitor\n\n@pytest.fixture\ndef monitor():\n    return PhenomenaMonitor()\n\n\ndef test_add_phenomenon_and_retrieve_by_type(monitor):\n    monitor.add_phenomenon({"type": "Aurora", "severity": "low", "observation_notes": []})\n    monitor.add_phenomenon({"type": "Supernova", "severity": "high", "observation_notes": []})\n    auroras = monitor.get_phenomena_by_type("Aurora")\n    assert len(auroras) == 1\n    assert auroras[0]["type"] == "Aurora"\n\ndef test_get_severity_count(monitor):\n    monitor.add_phenomenon({"type": "Aurora", "severity": "low", "observation_notes": []})\n    monitor.add_phenomenon({"type": "Supernova", "severity": "high", "observation_notes": []})\n    monitor.add_phenomenon({"type": "Meteor Shower", "severity": "medium", "observation_notes": []})\n    severity_count = monitor.get_severity_count()\n    assert severity_count == {"low": 1, "medium": 1, "high": 1}\n\ndef test_add_observation(monitor):\n    monitor.add_phenomenon({"type": "Aurora", "severity": "low", "observation_notes": []})\n    monitor.add_observation(0, "Observed during night shift.")\n    auroras = monitor.get_phenomena_by_type("Aurora")\n    assert auroras[0]["observation_notes"] == ["Observed during night shift."]\n\ndef test_add_observation_invalid_index_gracefully(monitor):\n    monitor.add_phenomenon({"type": "Aurora", "severity": "low", "observation_notes": []})\n    monitor.add_observation(10, "Impossible observation.")  # Invalid index\n    auroras = monitor.get_phenomena_by_type("Aurora")\n    assert auroras[0]["observation_notes"] == []\n\ndef test_empty_severity_count(monitor):\n    severity_count = monitor.get_severity_count()\n    assert severity_count == {}', solution_program="class PhenomenaMonitor:\n    def __init__(self):\n        self.phenomena = []\n\n    def add_phenomenon(self, phenomenon):\n        self.phenomena.append(phenomenon)\n\n    def get_phenomena_by_type(self, phenomenon_type):\n        return [p for p in self.phenomena if p['type'] == phenomenon_type]\n\n    def get_severity_count(self):\n        severity_count = {}\n        for phenomenon in self.phenomena:\n            severity = phenomenon['severity']\n            if severity in severity_count:\n                severity_count[severity] += 1\n            else:\n                severity_count[severity] = 1\n        return severity_count\n\n    def add_observation(self, phenomenon_index, note):\n        try:\n            self.phenomena[phenomenon_index]['observation_notes'].append(note)\n        except IndexError:\n            pass"), annotations=[]), ParsedChatCompletionMessage[ProgrammingProblem](content='{"task_description":"### Science Fiction Adventure Management System\\n\\nIn this programming task, you are required to implement a simple adventure management system that caters to science fiction enthusiasts. In this world, there exist various alien races, each with unique abilities and characteristics. Your task is to model an adventure management system that helps manage missions involving these aliens.\\n\\n#### Structure\\n- **Alien Class**: You need to implement a class `Alien` that encapsulates information and functionalities related to an alien character. Each `Alien` instance has a `name` (string), `species` (string), and a `skills` dictionary where keys are skill names and values are the proficiency levels.\\n\\n- **Mission Management**: Implement a function `assign_mission(aliens, mission_requirements)` where `aliens` is a list of `Alien` objects, and `mission_requirements` is a dictionary with skill names as keys and minimum skill levels required as values.\\n  - The `assign_mission` function should return a list of aliens who meet or exceed the skills listed in `mission_requirements`. If an alien does not have a required skill, assume its proficiency is 0.\\n  - If no aliens can be assigned to a mission, the function should raise a `ValueError` with the message \\"No suitable aliens found\\".\\n\\n#### Requirements\\n- **Selection**: Use condition checks to determine if an alien satisfies the mission requirements.\\n- **Exception Handling**: Raise an exception if no appropriate aliens can be assigned to the mission.\\n- **Objects and Classes**: Use classes to encapsulate the alien data and functionalities.\\n\\n#### Example\\nSuppose there is a mission that requires \'hacking\' skill level of at least 5 and \'flying\' skill level of at least 7. You should implement the `assign_mission` function such that it correctly identifies which aliens can be assigned to this mission, based on their skill levels in those areas. The solution should be robust and handle cases where no aliens are suitable for the mission.","test_suite":"import pytest\\nfrom solution import Alien, assign_mission\\n\\ndef test_all_aliens_qualified():\\n    alien1 = Alien(\\"Zog\\", \\"Martian\\", {\\"hacking\\": 5, \\"flying\\": 8})\\n    alien2 = Alien(\\"Xena\\", \\"Venusian\\", {\\"hacking\\": 6, \\"flying\\": 8})\\n    aliens = [alien1, alien2]\\n    mission_requirements = {\\"hacking\\": 5, \\"flying\\": 7}\\n    result = assign_mission(aliens, mission_requirements)\\n    assert result == [alien1, alien2]\\n\\ndef test_some_aliens_qualified():\\n    alien1 = Alien(\\"Zog\\", \\"Martian\\", {\\"hacking\\": 4, \\"flying\\": 9})\\n    alien2 = Alien(\\"Xena\\", \\"Venusian\\", {\\"hacking\\": 6, \\"flying\\": 2})\\n    alien3 = Alien(\\"Glorp\\", \\"Jupiterian\\", {\\"hacking\\": 5, \\"flying\\": 8})\\n    aliens = [alien1, alien2, alien3]\\n    mission_requirements = {\\"hacking\\": 5, \\"flying\\": 7}\\n    result = assign_mission(aliens, mission_requirements)\\n    assert result == [alien3]\\n\\ndef test_no_aliens_qualified():\\n    alien1 = Alien(\\"Zog\\", \\"Martian\\", {\\"hacking\\": 4, \\"flying\\": 6})\\n    alien2 = Alien(\\"Xena\\", \\"Venusian\\", {\\"hacking\\": 5, \\"flying\\": 5})\\n    aliens = [alien1, alien2]\\n    mission_requirements = {\\"hacking\\": 5, \\"flying\\": 7}\\n    with pytest.raises(ValueError, match=\\"No suitable aliens found\\"):\\n        assign_mission(aliens, mission_requirements)\\n\\ndef test_no_skills_listed_yet_met():\\n    alien1 = Alien(\\"Zog\\", \\"Martian\\", {})\\n    alien2 = Alien(\\"Xena\\", \\"Venusian\\", {\\"hacking\\": 6, \\"flying\\": 2})\\n    aliens = [alien1, alien2]\\n    mission_requirements = {\\"hacking\\": 5}\\n    result = assign_mission(aliens, mission_requirements)\\n    assert result == [alien2]\\n\\n\\ndef test_empty_alien_list():\\n    aliens = []\\n    mission_requirements = {\\"hacking\\": 5, \\"flying\\": 7}\\n    with pytest.raises(ValueError, match=\\"No suitable aliens found\\"):\\n        assign_mission(aliens, mission_requirements)","solution_program":"class Alien:\\n    def __init__(self, name, species, skills):\\n        self.name = name\\n        self.species = species\\n        self.skills = skills\\n\\n    def has_skills_for_mission(self, mission_requirements):\\n        for skill, min_level in mission_requirements.items():\\n            if self.skills.get(skill, 0) < min_level:\\n                return False\\n        return True\\n\\ndef assign_mission(aliens, mission_requirements):\\n    eligible_aliens = []\\n    for alien in aliens:\\n        if alien.has_skills_for_mission(mission_requirements):\\n            eligible_aliens.append(alien)\\n    if not eligible_aliens:\\n        raise ValueError(\\"No suitable aliens found\\")\\n    return eligible_aliens"}', refusal=None, role='assistant', function_call=None, tool_calls=[], parsed=ProgrammingProblem(task_description='### Science Fiction Adventure Management System\n\nIn this programming task, you are required to implement a simple adventure management system that caters to science fiction enthusiasts. In this world, there exist various alien races, each with unique abilities and characteristics. Your task is to model an adventure management system that helps manage missions involving these aliens.\n\n#### Structure\n- **Alien Class**: You need to implement a class `Alien` that encapsulates information and functionalities related to an alien character. Each `Alien` instance has a `name` (string), `species` (string), and a `skills` dictionary where keys are skill names and values are the proficiency levels.\n\n- **Mission Management**: Implement a function `assign_mission(aliens, mission_requirements)` where `aliens` is a list of `Alien` objects, and `mission_requirements` is a dictionary with skill names as keys and minimum skill levels required as values.\n  - The `assign_mission` function should return a list of aliens who meet or exceed the skills listed in `mission_requirements`. If an alien does not have a required skill, assume its proficiency is 0.\n  - If no aliens can be assigned to a mission, the function should raise a `ValueError` with the message "No suitable aliens found".\n\n#### Requirements\n- **Selection**: Use condition checks to determine if an alien satisfies the mission requirements.\n- **Exception Handling**: Raise an exception if no appropriate aliens can be assigned to the mission.\n- **Objects and Classes**: Use classes to encapsulate the alien data and functionalities.\n\n#### Example\nSuppose there is a mission that requires \'hacking\' skill level of at least 5 and \'flying\' skill level of at least 7. You should implement the `assign_mission` function such that it correctly identifies which aliens can be assigned to this mission, based on their skill levels in those areas. The solution should be robust and handle cases where no aliens are suitable for the mission.', test_suite='import pytest\nfrom solution import Alien, assign_mission\n\ndef test_all_aliens_qualified():\n    alien1 = Alien("Zog", "Martian", {"hacking": 5, "flying": 8})\n    alien2 = Alien("Xena", "Venusian", {"hacking": 6, "flying": 8})\n    aliens = [alien1, alien2]\n    mission_requirements = {"hacking": 5, "flying": 7}\n    result = assign_mission(aliens, mission_requirements)\n    assert result == [alien1, alien2]\n\ndef test_some_aliens_qualified():\n    alien1 = Alien("Zog", "Martian", {"hacking": 4, "flying": 9})\n    alien2 = Alien("Xena", "Venusian", {"hacking": 6, "flying": 2})\n    alien3 = Alien("Glorp", "Jupiterian", {"hacking": 5, "flying": 8})\n    aliens = [alien1, alien2, alien3]\n    mission_requirements = {"hacking": 5, "flying": 7}\n    result = assign_mission(aliens, mission_requirements)\n    assert result == [alien3]\n\ndef test_no_aliens_qualified():\n    alien1 = Alien("Zog", "Martian", {"hacking": 4, "flying": 6})\n    alien2 = Alien("Xena", "Venusian", {"hacking": 5, "flying": 5})\n    aliens = [alien1, alien2]\n    mission_requirements = {"hacking": 5, "flying": 7}\n    with pytest.raises(ValueError, match="No suitable aliens found"):\n        assign_mission(aliens, mission_requirements)\n\ndef test_no_skills_listed_yet_met():\n    alien1 = Alien("Zog", "Martian", {})\n    alien2 = Alien("Xena", "Venusian", {"hacking": 6, "flying": 2})\n    aliens = [alien1, alien2]\n    mission_requirements = {"hacking": 5}\n    result = assign_mission(aliens, mission_requirements)\n    assert result == [alien2]\n\n\ndef test_empty_alien_list():\n    aliens = []\n    mission_requirements = {"hacking": 5, "flying": 7}\n    with pytest.raises(ValueError, match="No suitable aliens found"):\n        assign_mission(aliens, mission_requirements)', solution_program='class Alien:\n    def __init__(self, name, species, skills):\n        self.name = name\n        self.species = species\n        self.skills = skills\n\n    def has_skills_for_mission(self, mission_requirements):\n        for skill, min_level in mission_requirements.items():\n            if self.skills.get(skill, 0) < min_level:\n                return False\n        return True\n\ndef assign_mission(aliens, mission_requirements):\n    eligible_aliens = []\n    for alien in aliens:\n        if alien.has_skills_for_mission(mission_requirements):\n            eligible_aliens.append(alien)\n    if not eligible_aliens:\n        raise ValueError("No suitable aliens found")\n    return eligible_aliens'), annotations=[]), ParsedChatCompletionMessage[ProgrammingProblem](content='{"task_description":"In a distant future, science fiction has become reality with the rise of autonomous spaceships capable of interstellar travel. Your task is to create a simple simulation of a fleet of spaceships exploring a galaxy. Each spaceship can have a name, fuel level, and a list of destinations it needs to explore. These ships also rely on a central command system to pass or fail diagnostics based on their fuel levels.\\n\\n1. Implement a `Spaceship` class with the following:\\n   - A constructor that initializes the name, fuel level, and a list of destinations for the spaceship.\\n   - A method `add_fuel(amount)` that increases the fuel level by the specified amount.\\n   - A method `travel(destination)` that attempts to travel to the given destination. If the destination exists on the spaceship\'s list, it removes it from the list and decreases the fuel level by 10 units. If not enough fuel is available, it should raise an exception.\\n\\n2. Implement a `Fleet` class with the following:\\n   - A constructor that initializes an empty list of spaceships.\\n   - A method `add_spaceship(spaceship)` that adds a spaceship object to the fleet.\\n   - A method `diagnostic()` that returns a dictionary with spaceship names as keys. The values should be \'PASS\' or \'FAIL\' based on whether their fuel level is above 50 units.\\n\\n3. Handle exceptions gracefully using try-except blocks, particularly when trying to travel to a destination without enough fuel.\\n\\nCreate these classes in a module named `galaxy_explorer`. The code will be tested using different scenarios to ensure that spaceships are correctly added to the fleet, fuel is added, travel can be attempted with proper exception handling, and the diagnostics system accurately reports the status of each spaceship.\\n\\nFunction signatures to be implemented:\\n- class `Spaceship`\\n- def `__init__(self, name, fuel_level, destinations)`\\n- def `add_fuel(self, amount)`\\n- def `travel(self, destination)`\\n- class `Fleet`\\n- def `__init__(self)`\\n- def `add_spaceship(self, spaceship)`\\n- def `diagnostic(self)`","test_suite":"import pytest\\nfrom galaxy_explorer import Spaceship, Fleet\\n\\n\\ndef test_spaceship_creation():\\n    ship = Spaceship(\\"Voyager\\", 100, [\\"Mars\\", \\"Jupiter\\"])\\n    assert ship.name == \\"Voyager\\"\\n    assert ship.fuel_level == 100\\n    assert ship.destinations == [\\"Mars\\", \\"Jupiter\\"]\\n\\n\\ndef test_add_fuel():\\n    ship = Spaceship(\\"Odyssey\\", 40, [])\\n    ship.add_fuel(20)\\n    assert ship.fuel_level == 60\\n\\n\\ndef test_travel_success():\\n    ship = Spaceship(\\"Endeavour\\", 100, [\\"Saturn\\", \\"Titan\\"])\\n    ship.travel(\\"Saturn\\")\\n    assert ship.fuel_level == 90\\n    assert ship.destinations == [\\"Titan\\"]\\n\\n\\ndef test_travel_insufficient_fuel():\\n    ship = Spaceship(\\"Curiosity\\", 5, [\\"Venus\\"])\\n    with pytest.raises(Exception):\\n        ship.travel(\\"Venus\\")\\n\\n\\ndef test_diagnostic():\\n    ship1 = Spaceship(\\"Pioneer\\", 60, [])\\n    ship2 = Spaceship(\\"Explorer\\", 45, [])\\n    fleet = Fleet()\\n    fleet.add_spaceship(ship1)\\n    fleet.add_spaceship(ship2)\\n    result = fleet.diagnostic()\\n    assert result == {\\"Pioneer\\": \\"PASS\\", \\"Explorer\\": \\"FAIL\\"}","solution_program":"class Spaceship:\\n    def __init__(self, name, fuel_level, destinations):\\n        self.name = name\\n        self.fuel_level = fuel_level\\n        self.destinations = destinations\\n\\n    def add_fuel(self, amount):\\n        self.fuel_level += amount\\n\\n    def travel(self, destination):\\n        if destination not in self.destinations:\\n            raise Exception(\\"Destination not found\\")\\n        if self.fuel_level < 10:\\n            raise Exception(\\"Insufficient fuel\\")\\n        self.fuel_level -= 10\\n        self.destinations.remove(destination)\\n\\n\\nclass Fleet:\\n    def __init__(self):\\n        self.spaceships = []\\n\\n    def add_spaceship(self, spaceship):\\n        self.spaceships.append(spaceship)\\n\\n    def diagnostic(self):\\n        report = {}\\n        for ship in self.spaceships:\\n            if ship.fuel_level > 50:\\n                report[ship.name] = \'PASS\'\\n            else:\\n                report[ship.name] = \'FAIL\'\\n        return report"}', refusal=None, role='assistant', function_call=None, tool_calls=[], parsed=ProgrammingProblem(task_description="In a distant future, science fiction has become reality with the rise of autonomous spaceships capable of interstellar travel. Your task is to create a simple simulation of a fleet of spaceships exploring a galaxy. Each spaceship can have a name, fuel level, and a list of destinations it needs to explore. These ships also rely on a central command system to pass or fail diagnostics based on their fuel levels.\n\n1. Implement a `Spaceship` class with the following:\n   - A constructor that initializes the name, fuel level, and a list of destinations for the spaceship.\n   - A method `add_fuel(amount)` that increases the fuel level by the specified amount.\n   - A method `travel(destination)` that attempts to travel to the given destination. If the destination exists on the spaceship's list, it removes it from the list and decreases the fuel level by 10 units. If not enough fuel is available, it should raise an exception.\n\n2. Implement a `Fleet` class with the following:\n   - A constructor that initializes an empty list of spaceships.\n   - A method `add_spaceship(spaceship)` that adds a spaceship object to the fleet.\n   - A method `diagnostic()` that returns a dictionary with spaceship names as keys. The values should be 'PASS' or 'FAIL' based on whether their fuel level is above 50 units.\n\n3. Handle exceptions gracefully using try-except blocks, particularly when trying to travel to a destination without enough fuel.\n\nCreate these classes in a module named `galaxy_explorer`. The code will be tested using different scenarios to ensure that spaceships are correctly added to the fleet, fuel is added, travel can be attempted with proper exception handling, and the diagnostics system accurately reports the status of each spaceship.\n\nFunction signatures to be implemented:\n- class `Spaceship`\n- def `__init__(self, name, fuel_level, destinations)`\n- def `add_fuel(self, amount)`\n- def `travel(self, destination)`\n- class `Fleet`\n- def `__init__(self)`\n- def `add_spaceship(self, spaceship)`\n- def `diagnostic(self)`", test_suite='import pytest\nfrom galaxy_explorer import Spaceship, Fleet\n\n\ndef test_spaceship_creation():\n    ship = Spaceship("Voyager", 100, ["Mars", "Jupiter"])\n    assert ship.name == "Voyager"\n    assert ship.fuel_level == 100\n    assert ship.destinations == ["Mars", "Jupiter"]\n\n\ndef test_add_fuel():\n    ship = Spaceship("Odyssey", 40, [])\n    ship.add_fuel(20)\n    assert ship.fuel_level == 60\n\n\ndef test_travel_success():\n    ship = Spaceship("Endeavour", 100, ["Saturn", "Titan"])\n    ship.travel("Saturn")\n    assert ship.fuel_level == 90\n    assert ship.destinations == ["Titan"]\n\n\ndef test_travel_insufficient_fuel():\n    ship = Spaceship("Curiosity", 5, ["Venus"])\n    with pytest.raises(Exception):\n        ship.travel("Venus")\n\n\ndef test_diagnostic():\n    ship1 = Spaceship("Pioneer", 60, [])\n    ship2 = Spaceship("Explorer", 45, [])\n    fleet = Fleet()\n    fleet.add_spaceship(ship1)\n    fleet.add_spaceship(ship2)\n    result = fleet.diagnostic()\n    assert result == {"Pioneer": "PASS", "Explorer": "FAIL"}', solution_program='class Spaceship:\n    def __init__(self, name, fuel_level, destinations):\n        self.name = name\n        self.fuel_level = fuel_level\n        self.destinations = destinations\n\n    def add_fuel(self, amount):\n        self.fuel_level += amount\n\n    def travel(self, destination):\n        if destination not in self.destinations:\n            raise Exception("Destination not found")\n        if self.fuel_level < 10:\n            raise Exception("Insufficient fuel")\n        self.fuel_level -= 10\n        self.destinations.remove(destination)\n\n\nclass Fleet:\n    def __init__(self):\n        self.spaceships = []\n\n    def add_spaceship(self, spaceship):\n        self.spaceships.append(spaceship)\n\n    def diagnostic(self):\n        report = {}\n        for ship in self.spaceships:\n            if ship.fuel_level > 50:\n                report[ship.name] = \'PASS\'\n            else:\n                report[ship.name] = \'FAIL\'\n        return report'), annotations=[]), ParsedChatCompletionMessage[ProgrammingProblem](content='{"task_description":"In the year 2303, humans have discovered a plethora of alien species across the galaxy, each with unique characteristics. Your task is to design a system to manage and access information about these species efficiently using Python.\\n\\nYou will implement a class `AlienRegistry` that has the following methods:\\n\\n1. `add_species(species_name, characteristics)`: Adds a new alien species with the given name and characteristics to the registry. The `characteristics` parameter is a dictionary where keys are strings representing trait names, and values are the corresponding traits which are also strings.\\n\\n2. `get_species_info(species_name)`: Returns the characteristics of the given species as a dictionary. If the species does not exist, it should raise a `ValueError` with the message `\\"Species not found\\"`.\\n\\n3. `get_all_species()`: Returns a list of all species names in the registry, sorted in ascending order.\\n\\nDesign your class and methods to manage the existence checks for the species efficiently. The registry should not accept duplicate species names.\\n\\nTo complete this task, you will use lists to manage species names and dictionaries to manage the characteristics of each species. Implement any selection statements and exception handling as necessary to ensure the functionality and robustness of the system.","test_suite":"import pytest\\nfrom solution import AlienRegistry\\n\\ndef test_add_and_get_species_info():\\n    registry = AlienRegistry()\\n    registry.add_species(\\"Zog\\", {\\"color\\": \\"blue\\", \\"limbs\\": \\"tentacles\\"})\\n    info = registry.get_species_info(\\"Zog\\")\\n    assert info == {\\"color\\": \\"blue\\", \\"limbs\\": \\"tentacles\\"}\\n\\n    registry.add_species(\\"Quo\\", {\\"color\\": \\"green\\", \\"limbs\\": \\"arms\\"})\\n    info = registry.get_species_info(\\"Quo\\")\\n    assert info == {\\"color\\": \\"green\\", \\"limbs\\": \\"arms\\"}\\n\\n    with pytest.raises(ValueError) as excinfo:\\n        registry.get_species_info(\\"Nonexistent\\")\\n    assert str(excinfo.value) == \\"Species not found\\"\\n\\n\\ndef test_get_all_species():\\n    registry = AlienRegistry()\\n    registry.add_species(\\"Xenon\\", {\\"size\\": \\"large\\"})\\n    registry.add_species(\\"Ael\\", {\\"size\\": \\"small\\"})\\n    registry.add_species(\\"Zex\\", {\\"size\\": \\"medium\\"})\\n    species_list = registry.get_all_species()\\n    assert species_list == [\\"Ael\\", \\"Xenon\\", \\"Zex\\"]\\n\\n\\ndef test_add_duplicate_species():\\n    registry = AlienRegistry()\\n    registry.add_species(\\"Blor\\", {\\"color\\": \\"red\\"})\\n    registry.add_species(\\"Blor\\", {\\"color\\": \\"green\\"})  # Attempt to add duplicate\\n    info = registry.get_species_info(\\"Blor\\")\\n    assert info == {\\"color\\": \\"red\\"}  # Characteristics should not change\\n\\n\\ndef test_handle_empty_registry():\\n    registry = AlienRegistry()\\n    with pytest.raises(ValueError) as excinfo:\\n        registry.get_species_info(\\"Alpha\\")\\n    assert str(excinfo.value) == \\"Species not found\\"\\n    assert registry.get_all_species() == []","solution_program":"class AlienRegistry:\\n    def __init__(self):\\n        self.registry = {}\\n\\n    def add_species(self, species_name, characteristics):\\n        if species_name not in self.registry:\\n            self.registry[species_name] = characteristics\\n\\n    def get_species_info(self, species_name):\\n        if species_name in self.registry:\\n            return self.registry[species_name]\\n        else:\\n            raise ValueError(\\"Species not found\\")\\n\\n    def get_all_species(self):\\n        return sorted(self.registry.keys())"}', refusal=None, role='assistant', function_call=None, tool_calls=[], parsed=ProgrammingProblem(task_description='In the year 2303, humans have discovered a plethora of alien species across the galaxy, each with unique characteristics. Your task is to design a system to manage and access information about these species efficiently using Python.\n\nYou will implement a class `AlienRegistry` that has the following methods:\n\n1. `add_species(species_name, characteristics)`: Adds a new alien species with the given name and characteristics to the registry. The `characteristics` parameter is a dictionary where keys are strings representing trait names, and values are the corresponding traits which are also strings.\n\n2. `get_species_info(species_name)`: Returns the characteristics of the given species as a dictionary. If the species does not exist, it should raise a `ValueError` with the message `"Species not found"`.\n\n3. `get_all_species()`: Returns a list of all species names in the registry, sorted in ascending order.\n\nDesign your class and methods to manage the existence checks for the species efficiently. The registry should not accept duplicate species names.\n\nTo complete this task, you will use lists to manage species names and dictionaries to manage the characteristics of each species. Implement any selection statements and exception handling as necessary to ensure the functionality and robustness of the system.', test_suite='import pytest\nfrom solution import AlienRegistry\n\ndef test_add_and_get_species_info():\n    registry = AlienRegistry()\n    registry.add_species("Zog", {"color": "blue", "limbs": "tentacles"})\n    info = registry.get_species_info("Zog")\n    assert info == {"color": "blue", "limbs": "tentacles"}\n\n    registry.add_species("Quo", {"color": "green", "limbs": "arms"})\n    info = registry.get_species_info("Quo")\n    assert info == {"color": "green", "limbs": "arms"}\n\n    with pytest.raises(ValueError) as excinfo:\n        registry.get_species_info("Nonexistent")\n    assert str(excinfo.value) == "Species not found"\n\n\ndef test_get_all_species():\n    registry = AlienRegistry()\n    registry.add_species("Xenon", {"size": "large"})\n    registry.add_species("Ael", {"size": "small"})\n    registry.add_species("Zex", {"size": "medium"})\n    species_list = registry.get_all_species()\n    assert species_list == ["Ael", "Xenon", "Zex"]\n\n\ndef test_add_duplicate_species():\n    registry = AlienRegistry()\n    registry.add_species("Blor", {"color": "red"})\n    registry.add_species("Blor", {"color": "green"})  # Attempt to add duplicate\n    info = registry.get_species_info("Blor")\n    assert info == {"color": "red"}  # Characteristics should not change\n\n\ndef test_handle_empty_registry():\n    registry = AlienRegistry()\n    with pytest.raises(ValueError) as excinfo:\n        registry.get_species_info("Alpha")\n    assert str(excinfo.value) == "Species not found"\n    assert registry.get_all_species() == []', solution_program='class AlienRegistry:\n    def __init__(self):\n        self.registry = {}\n\n    def add_species(self, species_name, characteristics):\n        if species_name not in self.registry:\n            self.registry[species_name] = characteristics\n\n    def get_species_info(self, species_name):\n        if species_name in self.registry:\n            return self.registry[species_name]\n        else:\n            raise ValueError("Species not found")\n\n    def get_all_species(self):\n        return sorted(self.registry.keys())'), annotations=[]), ParsedChatCompletionMessage[ProgrammingProblem](content='{"task_description":"In a distant future, humans have developed a fleet of spaceships for intergalactic travel. You are tasked with creating a Python class `FleetManager` that manages a collection of spaceships, each having a name, a maximum speed, and a list of missions it has undertaken.\\n\\nYour implementation should include the following:\\n\\n- Implement a class `Ship` with attributes:\\n  - `name`: a string representing the ship\'s name.\\n  - `max_speed`: an integer representing the maximum speed of the ship.\\n  - `missions`: a list of strings representing the missions the ship has undertaken.\\n\\n- Implement a class `FleetManager` that manages a collection of `Ship` objects. It should have the following methods:\\n  \\n  - `add_ship(name, max_speed)`: Adds a new `Ship` with the specified name and max_speed to the fleet. If a ship with the same name already exists, it should raise a `ValueError`.\\n  \\n  - `record_mission(ship_name, mission)`: Records a new mission for the specified ship. If the ship does not exist in the fleet, it should raise a `KeyError`.\\n  \\n  - `get_missions(ship_name)`: Returns the list of missions for the specified ship. If the ship does not exist, it should return an empty list.\\n  \\n  - `get_fleet_summary()`: Returns a dictionary where keys are the names of the ships and values are dictionaries with two keys: `max_speed` and `missions`.\\n  \\nYour program will be tested using various scenarios to ensure that the fleet manager can handle querying and updating ship information as required.","test_suite":"import pytest\\nfrom solution import FleetManager\\n\\ndef test_add_ship_success():\\n    fm = FleetManager()\\n    fm.add_ship(\\"Enterprise\\", 1000)\\n    assert \\"Enterprise\\" in fm.get_fleet_summary()\\n\\n\\ndef test_add_ship_duplicate_error():\\n    fm = FleetManager()\\n    fm.add_ship(\\"Enterprise\\", 1000)\\n    with pytest.raises(ValueError):\\n        fm.add_ship(\\"Enterprise\\", 1200)\\n\\n\\ndef test_record_mission_success():\\n    fm = FleetManager()\\n    fm.add_ship(\\"Falcon\\", 900)\\n    fm.record_mission(\\"Falcon\\", \\"Mars Exploration\\")\\n    assert \\"Mars Exploration\\" in fm.get_missions(\\"Falcon\\")\\n\\n\\ndef test_record_mission_no_ship_error():\\n    fm = FleetManager()\\n    with pytest.raises(KeyError):\\n        fm.record_mission(\\"Voyager\\", \\"Jupiter Flyby\\")\\n\\n\\ndef test_get_fleet_summary_structure():\\n    fm = FleetManager()\\n    fm.add_ship(\\"Apollo\\", 800)\\n    fm.record_mission(\\"Apollo\\", \\"Moon Landing\\")\\n    summary = fm.get_fleet_summary()\\n    assert summary == {\\"Apollo\\": {\\"max_speed\\": 800, \\"missions\\": [\\"Moon Landing\\"]}}\\n\\n\\ndef test_get_missions_no_ship():\\n    fm = FleetManager()\\n    missions = fm.get_missions(\\"NonExistent\\")\\n    assert missions == []\\n","solution_program":"class Ship:\\n    def __init__(self, name, max_speed):\\n        self.name = name\\n        self.max_speed = max_speed\\n        self.missions = []\\n\\n\\nclass FleetManager:\\n    def __init__(self):\\n        self.ships = {}\\n\\n    def add_ship(self, name, max_speed):\\n        if name in self.ships:\\n            raise ValueError(\\"A ship with this name already exists.\\")\\n        self.ships[name] = Ship(name, max_speed)\\n\\n    def record_mission(self, ship_name, mission):\\n        if ship_name not in self.ships:\\n            raise KeyError(\\"No ship found with this name.\\")\\n        self.ships[ship_name].missions.append(mission)\\n\\n    def get_missions(self, ship_name):\\n        if ship_name not in self.ships:\\n            return []\\n        return self.ships[ship_name].missions\\n\\n    def get_fleet_summary(self):\\n        summary = {}\\n        for name, ship in self.ships.items():\\n            summary[name] = {\\n                \\"max_speed\\": ship.max_speed,\\n                \\"missions\\": ship.missions\\n            }\\n        return summary\\n"}', refusal=None, role='assistant', function_call=None, tool_calls=[], parsed=ProgrammingProblem(task_description="In a distant future, humans have developed a fleet of spaceships for intergalactic travel. You are tasked with creating a Python class `FleetManager` that manages a collection of spaceships, each having a name, a maximum speed, and a list of missions it has undertaken.\n\nYour implementation should include the following:\n\n- Implement a class `Ship` with attributes:\n  - `name`: a string representing the ship's name.\n  - `max_speed`: an integer representing the maximum speed of the ship.\n  - `missions`: a list of strings representing the missions the ship has undertaken.\n\n- Implement a class `FleetManager` that manages a collection of `Ship` objects. It should have the following methods:\n  \n  - `add_ship(name, max_speed)`: Adds a new `Ship` with the specified name and max_speed to the fleet. If a ship with the same name already exists, it should raise a `ValueError`.\n  \n  - `record_mission(ship_name, mission)`: Records a new mission for the specified ship. If the ship does not exist in the fleet, it should raise a `KeyError`.\n  \n  - `get_missions(ship_name)`: Returns the list of missions for the specified ship. If the ship does not exist, it should return an empty list.\n  \n  - `get_fleet_summary()`: Returns a dictionary where keys are the names of the ships and values are dictionaries with two keys: `max_speed` and `missions`.\n  \nYour program will be tested using various scenarios to ensure that the fleet manager can handle querying and updating ship information as required.", test_suite='import pytest\nfrom solution import FleetManager\n\ndef test_add_ship_success():\n    fm = FleetManager()\n    fm.add_ship("Enterprise", 1000)\n    assert "Enterprise" in fm.get_fleet_summary()\n\n\ndef test_add_ship_duplicate_error():\n    fm = FleetManager()\n    fm.add_ship("Enterprise", 1000)\n    with pytest.raises(ValueError):\n        fm.add_ship("Enterprise", 1200)\n\n\ndef test_record_mission_success():\n    fm = FleetManager()\n    fm.add_ship("Falcon", 900)\n    fm.record_mission("Falcon", "Mars Exploration")\n    assert "Mars Exploration" in fm.get_missions("Falcon")\n\n\ndef test_record_mission_no_ship_error():\n    fm = FleetManager()\n    with pytest.raises(KeyError):\n        fm.record_mission("Voyager", "Jupiter Flyby")\n\n\ndef test_get_fleet_summary_structure():\n    fm = FleetManager()\n    fm.add_ship("Apollo", 800)\n    fm.record_mission("Apollo", "Moon Landing")\n    summary = fm.get_fleet_summary()\n    assert summary == {"Apollo": {"max_speed": 800, "missions": ["Moon Landing"]}}\n\n\ndef test_get_missions_no_ship():\n    fm = FleetManager()\n    missions = fm.get_missions("NonExistent")\n    assert missions == []\n', solution_program='class Ship:\n    def __init__(self, name, max_speed):\n        self.name = name\n        self.max_speed = max_speed\n        self.missions = []\n\n\nclass FleetManager:\n    def __init__(self):\n        self.ships = {}\n\n    def add_ship(self, name, max_speed):\n        if name in self.ships:\n            raise ValueError("A ship with this name already exists.")\n        self.ships[name] = Ship(name, max_speed)\n\n    def record_mission(self, ship_name, mission):\n        if ship_name not in self.ships:\n            raise KeyError("No ship found with this name.")\n        self.ships[ship_name].missions.append(mission)\n\n    def get_missions(self, ship_name):\n        if ship_name not in self.ships:\n            return []\n        return self.ships[ship_name].missions\n\n    def get_fleet_summary(self):\n        summary = {}\n        for name, ship in self.ships.items():\n            summary[name] = {\n                "max_speed": ship.max_speed,\n                "missions": ship.missions\n            }\n        return summary\n'), annotations=[])]